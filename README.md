# Browser-OS

A browser based OS simulation built as a single HTML file.

Featuring a terminal, animated 3D background, and a modular desktop environment, all running in-browser with no dependencies beyond CDN-loaded libraries.
